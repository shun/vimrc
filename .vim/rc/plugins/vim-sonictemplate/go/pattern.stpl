^main$
	package main

	func main() {
		{{_cursor_}}
	}

iferr$
	if err != nil {
		log.Fatal(err)
	}
	{{_cursor_}}

fori$
	for i := 0; i < {{_cursor_}}; i++ {
	}

forj$
	for j := 0; j < {{_cursor_}}; j++ {
	}

fork$
	for k := 0; k < {{_cursor_}}; k++ {
	}

