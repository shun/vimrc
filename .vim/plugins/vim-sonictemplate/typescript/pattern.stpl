if\.\(\S\+\)$
	if ({{$1}} === {{_cursor_}}) {
	  
	}


if$
	if ({{_cursor_}}) {
	  
	}


ifel$
	if ({{_cursor_}}) {
	  
	
	} else {
	  
	
	}

for\.\(\S\+\)$
	for (let {{$1}} = 0; {{$1}} < {{_cursor_}}; ++{{$1}}) {
	  
	}

